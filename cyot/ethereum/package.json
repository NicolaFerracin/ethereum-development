{
  "name": "byot",
  "description": "Bring your own token - create an ERC20 token with a few simple clicks",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^4.5.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.5",
    "ethers": "^5.0.0",
    "hardhat": "^2.8.4"
  },
  "scripts": {
    "compile": "hardhat compile",
    "start-node": "hardhat node",
    "deploy": "hardhat run ./scripts/deploy.js && cp -R ./artifacts/contracts ../app"
  }
}
